#include "Jeu.h"
#include <unistd.h> 

int main(int argc, char const *argv[])
{
    /*Jeu j;
    while (true)
    {
        j.actionAutomatique(0.1);
        sleep(1);
        if(j.getConstEnnemi().getPositionX()==9 && j.getConstEnnemi().getPositionY()==3){
            break;
        }
        
    }
    */
    
    return 0;
}
